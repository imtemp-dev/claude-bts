# Build
bin/
dist/

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib

# Dependencies
node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Exception: VS Code snippets shipped as a template by `jig init`
!internal/template/templates/.vscode/
!internal/template/templates/.vscode/**

# OS
.DS_Store
Thumbs.db

# jig state (per-project, not committed)
.jig/local/

# Runtime state that collects INSIDE the template tree. state.FindRoot
# walks upward for a `.jig/`, and internal/template/templates/.jig is one,
# so any jig command or hook run with a working directory in there records
# its state under the templates. `//go:embed all:templates` would then bake
# one developer's metrics, tool traces and specs into the release binary,
# and `jig update` force-writes them over the user's own. The pattern above
# does not reach here: a pattern with a separator in the middle is anchored
# to the repository root. Allowlisted to config/, mirroring
# template.isRuntimeState.
internal/template/templates/.jig/*
!internal/template/templates/.jig/config/
!internal/template/templates/.jig/status_line.sh

# Prototype data
data/test-turns.json
data/eval-*.json

# Environment
.env
